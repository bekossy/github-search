import { Search } from "../searchBar/Search";

export const Layout = (props) => {
  const {
    avatar_url,
    bio,
    company,
    followers,
    following,
    created_at,
    name,
    login,
    public_repos,
    twitter_username,
    blog,
  } = props;
  return (
    <>
      <section>
        <h2>Layout</h2>
      </section>
    </>
  );
};
